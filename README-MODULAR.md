# עדכון מודולרי — מה בא לך לאכול אהובתי? ❤️

החבילה מחליפה את הקבצים:
- `index.html`
- `style.css`
- `sw.js`

ומוסיפה תיקייה:
- `js/`

## העלאה
1. חלץ את ה-ZIP.
2. העלה את כל התוכן לריפו.
3. אשר Replace לקבצים הקיימים.
4. Commit changes.

אין למחוק את התיקיות `images` ו-`icons` שכבר קיימות בריפו.
